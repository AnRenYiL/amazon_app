<form class="container" action="splitter" method="post">
    <div class="form-group">
        <label>Amount</label>
        <div class="input-group">
            <div class="input-group-prepend">
                <div class="input-group-text">$</div>
            </div>
            <input type="text" class="form-control" id="amount" name="amount">
        </div>
    </div>
    <div class="form-group">
        <label for="">Tax</label>
        <div class="input-group">
            <div class="input-group-prepend">
                <div class="input-group-text">%</div>
            </div>
            <input type="text" class="form-control" id="tax" name="tax">
        </div>
    </div>
    <div class="form-group">
        <label for="">Tip</label>
        <div class="input-group">
            <div class="input-group-prepend">
                <div class="input-group-text">%</div>
            </div>
            <input type="text" class="form-control" id="tip" name="tip">
        </div>
    </div>
    <div class="form-group">
        <label for="exampleInputEmail1">Number Of People</label>
        <input type="text" class="form-control" name="number" id="number">
    </div>
    <button type="submit" class="btn btn-primary">Submit</button>
     <% if params["amount"] %>
        $<%= ((params["amount"].to_i * (1 + params["tax"].to_i / 100.00 + params["tip"].to_i / 100.00))/params["number"].to_f).round(2) %>
    <% end %>
</form>
<!--<script>
    function btnClick() {
        const amount = parseFloat(document.getElementById("amount").value);
        const tax = parseFloat(document.getElementById("tax").value);
        const tip = parseFloat(document.getElementById("tip").value);
        const number = parseFloat(document.getElementById("number").value);
        let a = amount * (1 + tax / 100 + tip / 100) / number
        alert("Every one should pay $" + a.toFixed(2));
        document.getElementById("amount").value = "";
        document.getElementById("tax").value = "";
        document.getElementById("tip").value = "";
        document.getElementById("number").value = "";
        // return 0
    }
</script>-->